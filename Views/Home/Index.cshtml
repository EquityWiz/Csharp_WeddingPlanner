@{
    ViewData["Title"] = "Home Page";
}
@model User

<div class="container row flex justify-content-center">
    <div class="card p-3 text-white shadow mb-5 rounded col-4" style="background-color: rgba(108, 126, 130, .75);">
        <partial name="RegPartial"/>
        <div class="text-center">
            <p>
                Already a member? <a href="/login" class="link-primary text-center">Login</a>
            </p>
        </div>
    </div>
</div>
